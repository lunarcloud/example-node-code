---
# CodeQL configuration for Avalonia Node Editor
# Suppresses intentional generic catch clauses

name: "Avalonia Node Editor CodeQL Config"

# Query filters to disable specific queries for certain paths
query-filters: []
